package com.splitscale.reems.api.config.logging;

import org.springframework.context.annotation.Bean;
import org.springframework.context.annotation.Configuration;

import com.splitscale.reems.api.logging.LoggingInteractor;
import com.splitscale.reems.api.services.LoggingService;

@Configuration
public class LoggingServiceConfig {
  // @Bean
  // public LoggingInteractor getLoggingInteractor(LoggingService service) {
  // return new LoggingInteractor(service);
  // }

  // @Bean
  // public LoggingService getLoggingService(LoggingService service) {
  // return new LoggingService
  // }
}
